using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace ass18
{
    public class Test : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            // GameContainer<string> con1 = new GameContainer<string>();
            // con1.SetItem("Healing Potion");
            // Debug.Log(con1.GetItem());
            // Debug.Log(GameUtils.DescribeItem(con1.GetItem()));
        }

    }
}